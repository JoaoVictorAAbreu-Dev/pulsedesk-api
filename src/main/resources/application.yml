spring:
  application:
    name: pulsedesk-api
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/pulsedesk}
    driver-class-name: org.postgresql.Driver
    username: ${DATABASE_USERNAME:pulsedesk}
    password: ${DATABASE_PASSWORD:pulsedesk}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: never
app:
  security:
    jwt:
      secret: ${JWT_SECRET:pulsedesk-dev-secret-key-please-change-me}
      expiration-minutes: ${JWT_EXPIRATION_MINUTES:120}
server:
  port: 8081
